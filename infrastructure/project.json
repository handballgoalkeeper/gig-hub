{
  "name": "infrastructure",
  "projectType": "application",
  "sourceRoot": "infrastructure",
  "targets": {
    "infra-up": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.postgres.yml -f compose.redis.yml --env-file env/.env up -d"
      }
    },
    "infra-down": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.postgres.yml -f compose.redis.yml --env-file env/.env down"
      }
    },
    "infra-reset": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.postgres.yml -f composer.redis.yml --env-file env/.env down -v"
      }
    },
    "services-up": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.auth-service.yml -f compose.user-service.yml --env-file env/.env up --build -d"
      }
    },
    "services-down": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.auth-service.yml -f compose.user-service.yml --env-file env/.env down"
      }
    },
    "services-reset": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "infrastructure/docker",
        "command": "docker compose -f compose.base.yml -f compose.auth-service.yml -f compose.user-service.yml --env-file env/.env down -v"
      }
    }
  }
}
